{
  "name": "backend",
  "version": "1.0.0",
  "main": "./dist/onInit.js",
  "types": "./dist",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node build.js",
    "start": "nodemon --watch src --ext ts --exec \"node build.js && node dist/onInit.js\""
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "nodemon": "^3.1.7",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "express": "^4.19.2",
    "js-base64": "^3.7.7",
    "character-sheet-shared": "*"
  }
}
